# Dividend01.com SEO 优化项目书

**项目周期：** 2026-08-21 至 2026-11-19（90 天）  
**项目网站：** https://dividend01.com  
**核心市场：** 美国股息投资、股息 ETF、ETF dividend history 英文搜索流量  
**执行原则：** 先解决抓取与收录，再集中主题权重，最后扩大内容与外链。

## 一、项目背景与当前状态

Dividend01.com 已接入 Cloudflare Pages，并由 GitHub `main` 分支自动部署。当前生产提交为 `fb6ca2e`，CONY 与 QDTE 两篇新文章及其中文版本已经上线。

截至 2026-08-21，可确认的搜索状态如下：

- Google Search Console 属性为 `sc-domain:dividend01.com`，与正式站点一致。
- `https://dividend01.com/sitemap.xml` 已重新提交成功。
- Search Console 当前表格显示已发现 50 个网页；本次 sitemap 更新尚需等待 Google 重新读取。
- CONY 页面提交索引请求成功，提交时状态为“Google 无法识别此网址”。
- QDTE 页面已被 sitemap 发现，状态为“已发现－尚未编入索引”。
- Search Console 概览近期显示 14 次网页搜索点击，网站仍处于低权重、早期增长阶段。
- canonical、sitemap 与站点配置现统一使用非 `www` 主域 `https://dividend01.com`；`www` 仅作为重定向入口。
- 英文与中文页面已在 sitemap 中配置 `en`、`zh-CN` 和 `x-default` hreflang。

### 核心判断

当前主要瓶颈不是关键词数量不足，而是 Google 对网站的抓取频率、主题权威和页面价值信号仍然较弱。因此，短期不宜大规模铺量；应先提高现有重点页面的可发现性、内链权重、内容可信度和索引成功率。

## 二、项目目标

### 30 天目标

- sitemap 中的重点英文页面均可被 Google 发现。
- CONY、QDTE 及同期新增重点文章进入有效索引或完成问题诊断。
- 重点文章不存在孤立页面，距首页不超过 3 次点击。
- 完成主域、canonical、hreflang、robots 和 sitemap 的全站一致性验收。
- 建立 ETF Dividend History 主题中心与固定内链规则。

### 60 天目标

- 形成 1 个核心 Hub 与至少 8–12 篇高质量 Spoke 文章。
- 重点新页面 14 天内被发现，30 天内进入索引的比例达到 80% 以上。
- 非品牌自然展示量较项目启动基线增长 50% 以上。
- 至少 5 个目标词进入 Google 前 50 名。

### 90 天目标

- 建立稳定的“发布—发现—收录—排名—更新”内容闭环。
- 自然搜索点击较启动前 28 天增长 100% 以上。
- 至少 3 个低难度股息历史关键词进入前 20 名。
- 形成可复用的文章模板、数据更新规范、内链地图和月度复盘表。

以上为项目目标，不是对 Google 排名的保证；最终结果还受竞争强度、抓取节奏和外部权威信号影响。

## 三、优化范围与执行动作

### P0：抓取、索引与域名一致性（第 1–7 天）

1. 每日检查 Search Console sitemap 的“上次读取时间”和已发现页面数，确认本次更新被 Google 重新处理。
2. 导出并核对所有计划索引 URL：必须返回 200、允许抓取、自引用 canonical、无 `noindex`，且出现在 sitemap 中。
3. 验证以下规则全站一致：
   - HTTP → HTTPS 301；
   - `www` → 非 `www` 301；
   - canonical、Open Graph、JSON-LD、hreflang、robots 与 sitemap 全部使用 `https://dividend01.com`；
   - URL 尾部斜杠格式统一，不产生重定向链。
4. 对英中配对页面抽样检查 hreflang：自引用、双向返回、目标返回 200、各语言页面自 canonical，且 sitemap 与 HTML 声明不冲突。
5. 检查 sitemap 仅包含 canonical、可索引、返回 200 的页面，并自动更新 `lastmod`。
6. 对“已发现－尚未编入索引”页面优先补内链和独特价值，不连续重复提交索引请求。

**验收标准：** 无错误主机名；无重点页面 canonical/hreflang 冲突；所有重点 URL 在 3 次点击内可达；Search Console 能识别更新后的 sitemap。

### P1：站内结构与主题集群（第 1–21 天）

建立 `Dividend ETF History` 核心主题中心，形成 Hub → Spoke → Related Spoke 的结构。

首批重点页面：

- CONY Dividend History
- QDTE Dividend History
- JEPQ Dividend History
- JEPI Dividend History
- QYLD、RYLD、XYLD Dividend History
- XDTE 与其他周配 ETF 对比页
- Weekly Dividend ETF Calendar
- Return of Capital in Covered-Call ETFs

执行规则：

- 首页增加“Latest Dividend History”和“Weekly Dividend ETFs”入口。
- Hub 页面链接全部重点 Spoke；每篇 Spoke 链回 Hub，并链接 2–4 篇语义相关页面。
- 每篇新文章发布时，至少从 2 篇已有强相关页面反向链接。
- 锚文本以自然、描述性表达为主，避免所有链接机械重复精确关键词。
- 对没有有效内链、没有搜索需求或内容过薄的页面，选择合并、补强或暂缓索引。

**验收标准：** CONY、QDTE 等重点文章至少获得 3 条有效站内链接；无孤立重点页面；核心 Hub 距首页 1 次点击。

### P1：内容质量与搜索意图（第 8–60 天）

内容生产采用“低难度、明确意图、强主题相关”的策略，每周发布 2 篇，先稳定执行 8 周。暂不追求数量翻倍。

每篇文章必须包含：

- 关键词、Title、H1、URL 和首段意图一致；
- 最新分配频率、历史支付、收益来源、ROC 风险和投资者适用性；
- 数据日期、数据来源和更新说明；
- 原创表格、比较或计算示例，而非只改写公开资料；
- 1 张原创 WebP 配图，包含准确 alt 文本，并控制尺寸与加载方式；
- 清晰的相关内容内链和风险提示；
- 独立 title 与 meta description，避免模板化重复。

内容选题优先级：

1. 已有排名或展示、但排名 11–50 的词；
2. KD 较低、搜索意图明确、与现有 Hub 高度相关的词；
3. 能增强已有页面语义覆盖的比较词和问题词；
4. 高难度宽泛词仅作为长期 Hub，不作为短期流量预期。

**验收标准：** 每篇新内容有明确主关键词、唯一搜索意图、数据来源、原创视觉和至少 3 条相关内链；不存在同词互相竞争的页面。

### P1：E-E-A-T 与金融内容可信度（第 15–45 天）

- 建立清晰的作者页和编辑政策页，说明金融内容的研究、事实核对与更新流程。
- 完善 About、Contact、Privacy、Terms 和免责声明。
- 在文章中显示首次发布、最后更新日期和资料截止日期。
- 对收益率、分配历史和基金结构引用基金官网、SEC 文件或发行方资料。
- 明确区分“distribution”与“dividend”，避免将资本返还统一描述为股息。
- 对高收益 ETF 加入本金波动、期权覆盖、NAV 侵蚀和 ROC 等风险说明。

**验收标准：** 所有重点金融文章均具备作者、更新时间、来源与风险声明；抽样事实可回溯到一手资料。

### P2：页面体验、结构化数据与性能（第 22–60 天）

- 使用渲染后的页面验证 Article、BreadcrumbList、WebSite/Organization 等 JSON-LD，避免只检查静态抓取文本。
- 所有结构化数据中的 URL、图片、作者、日期与页面可见内容保持一致。
- 检查移动端 Core Web Vitals，目标为 LCP < 2.5 秒、INP < 200 毫秒、CLS < 0.1。
- 优化首屏图片、字体加载、缓存和不必要脚本；文章图片使用 WebP、明确宽高，并对非首屏图片懒加载。
- 检查移动端目录、表格、导航和点击目标，避免横向滚动。

**验收标准：** 重点模板无结构化数据错误；移动端无明显布局问题；核心指标达到良好或形成具体整改清单。

### P2：权威与外链（第 31–90 天）

- 优先制作可被引用的资产：ETF 分配历史表、周配 ETF 日历、ROC 解释图和可复用计算器。
- 向相关投资博客、ETF 社区和金融资料页推广可引用资产，而非批量购买链接。
- 建立品牌实体一致性：网站名称、作者身份、简介和社交资料描述统一。
- 记录新获得的引用域、链接目标页和自然增长效果。

**验收标准：** 90 天内获得首批真实相关引用域；外链主要指向 Hub、工具或数据资产，而非集中堆向单篇文章。

## 四、30/60/90 天排期

| 阶段 | 核心任务 | 主要交付物 |
|---|---|---|
| 第 1–7 天 | 收录与技术一致性 | URL 清单、索引状态表、canonical/hreflang/sitemap 验收表 |
| 第 8–21 天 | Hub 与内链结构 | 主题集群地图、首页模块、旧文反向内链、首批内容 |
| 第 22–30 天 | E-E-A-T 与模板完善 | 作者/编辑政策、来源规范、文章模板、首月报告 |
| 第 31–60 天 | 稳定内容与页面体验 | 8–12 篇集群内容、结构化数据验证、CWV 整改 |
| 第 61–90 天 | 更新、外链与增长复盘 | 内容刷新清单、可引用资产、外链记录、90 天报告 |

## 五、KPI 与数据看板

每周记录以下指标，并使用项目启动前 28 天作为基线：

- sitemap 已发现 URL 数；
- 已编入索引页面数与重点页面索引率；
- 新页面从发布到首次发现、首次抓取、首次展示的天数；
- 总展示、总点击、CTR、平均排名；
- 目标词前 50、前 20、前 10 的数量；
- 每个重点页面的站内入链数；
- 非品牌查询占比；
- Core Web Vitals 合格 URL 数；
- 新增相关引用域数量。

索引 KPI 应按“计划索引页面”计算，排除隐私页、工具状态页及明确不需要参与搜索的页面。

## 六、执行与发布流程

1. 从关键词数据和 Search Console 展示词中确定选题。
2. 检查站内是否已有同意图页面，防止关键词蚕食。
3. 完成资料研究、正文、原创配图、SEO 字段与内链。
4. 本地执行构建与 SEO 校验。
5. 提交并直接合并到 GitHub `main`。
6. Cloudflare Pages 自动构建部署。
7. 验证正式 URL、canonical、hreflang、图片和 sitemap。
8. 新重点文章请求索引，并进入每周跟踪表。
9. 14–30 天后根据展示词、CTR 和排名更新标题、段落与内链。

## 七、项目风险与控制

- **索引波动：** 新站收录存在延迟，不以重复提交代替页面质量和内链建设。
- **内容铺量风险：** 严禁为覆盖关键词批量发布高度相似页面。
- **金融事实风险：** 收益率和分配数据必须标明日期与来源，不作收益承诺。
- **多语言风险：** 中文页面必须完整翻译且具备独立价值，不能只翻译模板或跨语言 canonical。
- **域名风险：** 任何代码、数据或部署变更不得再次混用 `www` 与非 `www` 主域。
- **指标误判：** 以 28 天同比/环比趋势判断，不因单日抓取或排名波动频繁改版。

## 八、首周立即执行清单

- [ ] 确认 Google 已重新读取 2026-08-21 提交的 sitemap。
- [ ] 复查 CONY 与 QDTE 的索引状态及实际 URL 可索引性。
- [ ] 从首页、Hub 和至少 2 篇旧文为 CONY、QDTE 增加反向内链。
- [ ] 抽查 10 组英中页面的 canonical、hreflang、状态码和 sitemap 一致性。
- [ ] 建立 Search Console 周报表和重点 URL 索引跟踪表。
- [ ] 完成下一批 4 个低难度、同集群关键词的选题排序。
- [ ] 补齐作者、编辑政策、数据来源和更新说明模板。

## 九、项目结论

Dividend01.com 当前适合进入“技术稳定 + 集群成型”的阶段。未来 30 天的最高优先级是让 Google 稳定发现并认可现有重点页面；60 天内建立 ETF Dividend History 主题集群；90 天内通过数据资产、内容更新和真实引用扩大权威。只有在索引率和自然展示呈稳定增长后，才提高内容发布频率。
