# 2026-08-28 网站优化执行记录

## 今日范围

按多维方案执行本地修改，兼顾稳定性、技术 SEO、金融内容可信度、移动端体验、可访问性和性能。Cloudflare、GSC 和 GitHub 平台动作未在本次本地变更中执行。

## 已完成

- `BaseLayout.astro` 增加“跳到主要内容”入口，并为专题页补充“首页 → 专题 → 当前页”的 Breadcrumb 层级。
- 全局样式增加可见键盘焦点、跳转链接、触控最小尺寸、计算器错误态和 `prefers-reduced-motion` 支持。
- 英文计算器增加无效输入的可读错误提示，结果区域增加 `aria-live`，按钮明确为 `type="button"`。
- 中文计算器增加输入校验、错误提示和结果播报保持。
- 中文“股息投资基础”专题补充摘要、学习路径、基础概念、检查清单、FAQ 和下一步链接。
- 中文“股息股票筛选”专题补充筛选原则、行业指标、研究问题、FAQ 和工具路径。
- 保持既有规范 URL、尾斜杠、www 域名、hreflang 和 canonical 逻辑不变。

## 验证结果

- 使用项目要求的兼容 Node.js 运行时完成 `npm run build`：122 个页面构建成功。
- 生成 Sitemap：120 个规范 URL。
- SEO 校验通过：120 个可索引 HTML 页面；仅保留 1 条既有建议（中文 divisor 文章内部入链较少）。
- JSON-LD 审计通过：122 个 HTML 页面、0 个问题。
- `git diff --check` 通过。

## 待后续处理

- 对核心页面执行真实设备或浏览器的 360/390/768/1280 宽度、键盘和 200% 缩放检查，并形成 UX 审计报告。
- 评估 Google Fonts、响应式图片、LCP/INP/CLS 的线上数据。
- 英文专题 Hub 金融事实审计完成后，再决定是否继续扩写其余中文专题。
- GSC 只读复核和 404 验证必须以最新平台状态为准，不能因本地构建成功而直接提交。
- Git 提交和 GitHub 推送需单独审阅工作区文件清单后再执行。

## 预览反馈修复

- 发现分析同意条在显示后缺少样式，浏览器回退为原生按钮。
- 为 `#analytics-consent` 增加桌面和移动端卡片布局、按钮、焦点和层级样式，避免遮挡顶部导航。
- 重新构建验证通过：122 个页面、120 个规范 URL，SEO 校验通过。
- 在 472px 浏览器视口确认顶部中文切换入口和同意条均可见。
